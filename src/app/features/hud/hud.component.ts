import { ChangeDetectionStrategy, Component } from '@angular/core';

// NOTE: Stub — full HUD implementation (game year, speed controls, Kardashev bar) is deferred to the HUD block.
@Component({
  selector: 'app-hud',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: '',
})
export class HudComponent {}
