import { ChangeDetectionStrategy, Component } from '@angular/core';

// NOTE: Stub — full culture event toast implementation is deferred to the Culture Events block.
@Component({
  selector: 'app-culture-event-toast',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: '',
})
export class CultureEventToastComponent {}
