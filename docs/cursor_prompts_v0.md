# Helioscape — Foundation Prompts: UI, Theme, Localization, Audio, Debug
> Run these BEFORE any UI prompts in v2 or bio-tech files.
> Ordering matters — theme before UI, localization before any string is written,
> asset import settings before any texture is imported.

---

# STEP 0 — Before opening Godot

## `.gitignore` setup

```
Create a .gitignore file in the Helioscape project root for a Godot 4 project.

Exclude:
- .godot/          (auto-generated import cache — never commit)
- *.import         (per-asset import metadata — regenerated automatically)
- export_presets.cfg  (contains platform-specific paths, keep local)
- .DS_Store, Thumbs.db (OS cruft)

Do NOT exclude:
- *.tscn, *.tres    (scenes and resources — these are source files, must be committed)
- *.gd              (scripts — must be committed)
- data/*.json       (game data — must be committed)
- assets/           (all art, audio — must be committed)
- project.godot     (project settings — must be committed)

Add a comment at the top explaining that .godot/ is safe to delete and will be
regenerated by Godot on next open — it is not source, it is build cache.
```

---

# STEP 1 — First thing in Godot: import settings

## Asset import defaults (do this in the Godot editor, not via Cursor)

```
Before importing any textures, configure these project-wide import defaults in Godot.

In the Godot editor:
1. Go to Project → Project Settings → Import Defaults → Texture2D
2. Set the following:
   - compress/mode: Lossless (not VRAM Compressed — pixel art needs exact pixel values)
   - process/fix_alpha_border: false (prevents pixel edge bleeding)
   - process/premult_alpha: false
   - mipmaps/generate: false  ← critical for pixel art (mipmaps blur small textures)
   - texture/filter: Nearest  ← critical (Linear filtering = blurry pixels)
   - texture/repeat: Disabled (default)

3. Save the project.

This applies to all subsequently imported textures. Already-imported textures need
their .import files updated manually (reimport with these settings).

WHY THIS MATTERS: Godot's default Linear filter + mipmaps makes pixel art look blurry.
With Nearest filter and no mipmaps, every pixel is crisp at any scale. Since Helioscape
uses a post-process pixel shader on top of 3D, the underlying 3D textures should also
be nearest-filtered to stay consistent with the aesthetic.

For audio: Project → Project Settings → Import Defaults → AudioStream
   - loop: false (default off, enable per-file for music tracks only)
```

---

# STEP 2 — Theme resource

## `UI_GUIDE.md` (add to Cursor context for every UI prompt)

```
Create UI_GUIDE.md in the project root. This file is added to Cursor context for every
UI prompt. It defines all constraints, spacing decisions, and interaction patterns.
Cursor must follow these rules in every UI file generated.

---

# Helioscape UI Guide

## Design intent
Helioscape's UI is calm, readable, and warm. The aesthetic is "space-age paperwork" —
functional forms that have been lived in for decades. Not sterile. Not flashy.
The pixel art visual style (via post-process shader) applies to the 3D game world only.
All UI panels and overlays are NOT pixelated — they sit above the SubViewport in a
separate CanvasLayer and render at full resolution.

## Spatial system
Base unit: 8px. All padding and margins are multiples of 8.
- Tight (within a component): 8px
- Normal (between components): 16px
- Loose (between sections): 24px
- Panel internal padding: 16px on all sides
- Panel gap from screen edge: 16px

Use theme constants for all spacing — never hardcode pixel values in scene files.
If a value isn't in the theme, add it there before using it.

## Typography
Two font roles only:
- MONO: monospaced font (used for all data — numbers, years, resource counts, tech names)
- BODY: readable sans-serif (used for narrator text, descriptions, tooltips)

Size scale:
- xs: 11px (fine print, completion years in history)
- sm: 13px (secondary labels, status tags)
- md: 14px (default body, most labels)    ← base size
- lg: 16px (section headers, planet names)
- xl: 20px (panel titles, phase names)
- 2xl: 28px (year display in HUD)

Never use Godot's default Label font. Theme defines both fonts as DynamicFont resources.
Narrator text in CultureEventCard always uses BODY md. Data always uses MONO sm.

## Colour vocabulary
Define all colours as named Color constants in the theme. Never hardcode hex in scripts.

Background palette (dark, warm-tinted):
  bg_base:     #0d0d0f    (main background — near-black with slight warm tint)
  bg_surface:  #161618    (panel surfaces — one step lighter)
  bg_elevated: #1e1e22    (cards, dropdowns — two steps lighter)
  bg_overlay:  #000000aa  (modal overlays — semi-transparent)

Text palette:
  text_primary:   #e8e0d4  (primary text — warm off-white, not pure white)
  text_secondary: #8a8070  (secondary, labels, hints)
  text_disabled:  #4a4440  (disabled states)

Accent palette (warm amber — primary action, progress, highlights):
  accent:          #c8861e  (primary accent — amber)
  accent_dim:      #8a5c14  (dimmed accent — inactive progress)
  accent_glow:     #e8a030  (brighter — hover states, active elements)

Status palette:
  status_good:    #5a9e5a  (green — compatible pairs, healthy states)
  status_warn:    #c8a020  (amber-yellow — warnings, instability)
  status_bad:     #9e3a2a  (red — incompatible pairs, collapse, locked)
  status_neutral: #4a6a8a  (blue-grey — neutral info, locked-but-coming)

Planet accent colours (used in planet panel headers, orbit rings):
  planet_earth:   #3a7ab8
  planet_mercury: #8a7a5a
  planet_mars:    #b84a2a
  planet_venus:   #c8a030

Tag colours:
  tag_naturalist: #4a8a4a  (green — leaf icon)
  tag_architect:  #4a6ab8  (blue — gear icon)
  tag_neutral:    #8a8070  (grey — dot icon)

## Components

### Panels
All panels: bg_surface background, 4px corner radius, no border by default.
Active/selected panel: 1px accent border.
Locked panel: bg_base background, text_disabled text.
Panel titles: MONO lg, text_primary.

### Buttons
Primary (commit, confirm): accent background, bg_base text, 4px radius.
  Hover: accent_glow background. Disabled: bg_elevated background, text_disabled text.
Secondary (cancel, back, minor actions): bg_elevated background, text_primary text, 1px accent_dim border.
Destructive (irreversible actions — polar detonation confirm, Europa authorise):
  status_bad background. ALWAYS requires a second confirm dialog.

### Progress bars
Background: bg_elevated. Fill: accent. Height: 8px (thin) or 16px (prominent).
Stability meter in EcosystemComposer: colour-transitions —
  fill colour = lerp(status_bad, status_warn, status_good) based on percent.

### Status tags (ConditionsPanel, organism cards)
Small pill shape: 4px radius, 4px horizontal padding, 2px vertical padding.
  status_good background + text, status_warn background + text, status_bad background + text.
Text: MONO xs, always uppercase.

### Cards (organism cards, tech nodes, culture event toasts)
bg_elevated background, 4px radius, 8px internal padding.
Hover: 1px accent_dim border. Selected: 1px accent border.
Locked: bg_surface background, text_disabled text, silhouette treatment (50% opacity image).

## Interaction patterns

### Selection
Click to select. Click again to deselect. Never require double-click.
Selected state: 1px accent border + bg_elevated background.
Keyboard: Tab cycles focus, Space/Enter confirms, Escape cancels/deselects.

### Confirmations
Any action that costs resources: show cost summary in button tooltip (hover).
Any permanent/irreversible action: show a modal confirm dialog with:
  - Action description in BODY md
  - "This cannot be undone." in status_bad sm
  - Two buttons: [Confirm — destructive style] [Cancel — secondary style]
  Examples: committing a bio phase, authorising Europa impact, polar detonation.

### Tooltips
Every element with a condition, cost, or game mechanic should have a tooltip.
Tooltip delay: 0.6 seconds. Tooltip style: bg_overlay background, 8px padding, BODY sm.
Tech nodes: show prerequisites (met = status_good, unmet = status_bad), cost, duration.
Organism cards: show full compatible_with and incompatible_with lists.
Booster slots: show availability condition if unmet.

### Loading states
Any operation that takes a frame (loading a panel): show bg_surface panel immediately,
populate content in the next frame. Never show an empty flash.

### Empty states
Categories with no available organisms (OrganismPalette): show the category header
in text_disabled, with a single line beneath in text_secondary explaining why
(e.g. "Unlock Moon Tier 1 research to access engineered organisms").
Empty research queue: "No active research. Select a track to begin." in text_secondary.

## Godot-specific conventions

### Anchors
Panels that dock to an edge (HUD elements, PlanetsPanel): use anchor presets.
  Do not set anchors manually in code — set them in the scene editor.
Fullscreen overlays (EcosystemComposer, CultureEventCard): FULL_RECT anchor preset.

### Z-ordering (CanvasLayer order in Main.tscn)
Layer 0: SolarSystemViewport (3D scene, pixelated)
Layer 1: HUD (always visible — year, kardashev, time controls, planets panel)
Layer 2: PlanetPanel, EcosystemComposer (opened on demand)
Layer 3: CultureEventCard, CultureEventQueue toasts (above everything)
Layer 4: DebugConsole (above everything, including modals)
Modals (confirm dialogs): shown within their parent layer + bg_overlay behind them.

### No magic numbers in scripts
All spacing values: use theme.get_constant() or a const referencing the theme.
All colours: use theme.get_color() or a const. Never Color("#c8861e") inline.
All fonts: use theme.get_font() or a const.

### Control node hierarchy
Prefer VBoxContainer / HBoxContainer over manual positioning.
Use MarginContainer for internal panel padding (set to 16px on all sides).
Use separation property on VBox/HBox for gaps between children (8 or 16).
ScrollContainer wraps any list that might overflow (OrganismPalette, ResearchUI tracks).

### get_node() rules (from architecture)
No get_node() paths longer than one level in UI scripts.
Cache all child node refs in _ready() using @onready.

## What Cursor must never do
- Hardcode any colour, font size, or spacing value in a script or scene property.
- Use Godot's default theme colours (the blue buttons, grey panels).
- Create a button with only icon and no accessible label (accessibility).
- Use Label nodes for narrator text (use RichTextLabel — supports wrapping and bbcode).
- Add padding by inserting empty Control nodes (use MarginContainer or theme margins).
- Place UI nodes inside the SolarSystem SubViewport.
```

---

## Theme bootstrap Cursor prompt

```
Create the Godot 4 project theme for Helioscape.

Output: a GDScript file at src/tools/CreateTheme.gd that, when run as a tool script
(add @tool at the top), generates res://assets/theme/helioscape_theme.tres programmatically.

[→ Cursor: creating .tres files programmatically via @tool script is more maintainable than
 hand-editing binary .tres files. The script runs once, generates the theme, and can be
 re-run whenever design tokens change. Add a comment explaining this.]

The script must create a Theme resource and set the following. All values come from UI_GUIDE.md.

--- COLOURS ---
Register all colours from the UI_GUIDE.md colour vocabulary as named Color entries on the theme.
Use theme.set_color("accent", "Global", Color("#c8861e")) pattern — node_type "Global"
makes them accessible project-wide.

--- FONTS ---
Create two DynamicFont resources:
  font_mono: load from "res://assets/fonts/mono.ttf" (placeholder path — actual font TBD)
             size: 14 (md base size)
  font_body: load from "res://assets/fonts/body.ttf" (placeholder path)
             size: 14

Register as default_font on relevant node types:
  theme.set_font("font", "Label", font_mono)
  theme.set_font("font", "Button", font_mono)
  theme.set_font("font", "RichTextLabel", font_body)
  theme.set_font("font", "LineEdit", font_mono)

--- CONSTANTS (SPACING) ---
theme.set_constant("margin_tight", "Global", 8)
theme.set_constant("margin_normal", "Global", 16)
theme.set_constant("margin_loose", "Global", 24)
theme.set_constant("panel_padding", "Global", 16)
theme.set_constant("separation", "VBoxContainer", 16)
theme.set_constant("separation", "HBoxContainer", 8)

--- STYLEBOXES ---
Create StyleBoxFlat resources for each component state:

Panel (bg_surface, 4px corner radius):
  theme.set_stylebox("panel", "Panel", _make_panel_stylebox())

Button normal (bg_elevated background, 1px accent_dim border, 4px radius, 8px h-padding):
Button hover (bg_elevated + accent_dim border brightened to accent):
Button pressed (bg_surface background):
Button disabled (bg_base background, no border):
  [→ Cursor: create helper functions _make_button_normal(), _make_button_hover(), etc.
   Register all four: theme.set_stylebox("normal"|"hover"|"pressed"|"disabled", "Button", ...)]

Primary button variant — use a separate StyleBox registered under "ButtonPrimary" node type:
  Normal: accent background, 4px radius
  Hover: accent_glow background
  Disabled: bg_elevated, no border

Destructive button variant — registered under "ButtonDestructive":
  Normal: status_bad background

ProgressBar:
  background: bg_elevated StyleBox, 8px height
  fill: accent StyleBox

PanelContainer (used for cards and elevated surfaces):
  bg_elevated background, 4px radius, no border

--- SAVE ---
At the end of the script:
  ResourceSaver.save(theme, "res://assets/theme/helioscape_theme.tres")
  print("Theme saved.")

After running this script:
1. Open Project Settings → General → GUI → Theme → Custom
2. Set to res://assets/theme/helioscape_theme.tres
This makes it the project-wide default.

[→ Cursor: add a README comment at the top of CreateTheme.gd listing these two manual steps,
 since the script cannot set the project default theme automatically.]
```

---

# STEP 3 — Localization

## `data/localization/en.csv` and localization setup

```
Set up Godot 4's localization system for Helioscape. Two parts: the CSV file and the code conventions.

--- PART 1: localization CSV ---

Create data/localization/en.csv.

Godot's CSV format: first column = key, subsequent columns = translations.
First row: keys,en
Subsequent rows: KEY,English text

[→ Cursor: explain in a comment that all user-facing strings go through tr("KEY"). The key
 is SCREAMING_SNAKE_CASE. The English text is the actual display string. When additional
 languages are added later, new columns are added and Godot handles the rest automatically.]

Seed the file with keys for all HUD and structural UI strings (not culture event texts — those
are loaded from culture_events.json and passed through tr() at display time with their event_id
as the key). Include:

UI structure:
  UI_YEAR_LABEL, "Year {year}"
  UI_PAUSE, "Pause"
  UI_PLAY, "Play"
  UI_SPEED_1X, "1×"
  UI_SPEED_5X, "5×"
  UI_PLANET_LOCKED, "Humanity hasn't reached this far yet."
  UI_RESEARCH_EMPTY, "No active research. Select a track to begin."
  UI_RP_DISPLAY, "{used} / {total} RP in use"
  UI_YEARS_REMAINING, "~{n} years"
  UI_TECH_COST, "{rp} RP · {years} years"
  UI_COMMIT_READY, "Commit composition"
  UI_COMMIT_NEEDS_SLOTS, "Fill all required slots to commit"
  UI_PREVIEW_COMPOSITION, "Preview composition"
  UI_BACK_TO_PALETTE, "Back to palette"

Status tags (ConditionsPanel):
  STATUS_EXTREME_COLD, "Extreme cold — cryophilic organisms only"
  STATUS_SEVERE_COLD, "Severe cold — adapted pioneers viable"
  STATUS_COLD, "Cold — tundra-specialist organisms"
  STATUS_TEMPERATE, "Temperate — broad organism tolerance"
  STATUS_WARM, "Warm — full palette available"
  STATUS_EXTREME_HEAT, "Extreme heat — most organisms non-viable"
  STATUS_TRACE_ATMOSPHERE, "Trace atmosphere — nearly vacuum"
  STATUS_THIN_ATMOSPHERE, "Thin atmosphere — high-altitude Earth equivalent"
  STATUS_BUILDING_ATMOSPHERE, "Building — some weather developing"
  STATUS_SUBSTANTIAL_ATMOSPHERE, "Substantial atmosphere — pressure suit optional"
  STATUS_EXTREME_PRESSURE, "Extreme pressure — crushing at surface"
  STATUS_HIGH_PRESSURE, "High pressure — survivable in sealed habitat"
  STATUS_SOIL_RAW_REGOLITH, "Raw regolith — no organic matter, extreme pH"
  STATUS_SOIL_EARLY_MINERALS, "Early mineral chemistry developing"
  STATUS_SOIL_BASIC, "Basic soil established"
  STATUS_SOIL_COMPLEX, "Growing soil complexity"
  STATUS_NO_LIQUID_WATER, "No liquid water — aquatic organisms unavailable"
  STATUS_FRESH_OCEAN, "Fresh to slightly saline — broad microbial tolerance"
  STATUS_ACID_OCEAN, "Residual acidity — sulphur-tolerant organisms recommended"

Stability meter annotations (ProjectionPanel):
  STABILITY_NO_DECOMPOSERS, "No decomposers — organic matter will accumulate and choke photosynthesis around Year ~12"
  STABILITY_NO_HOST_PLANT, "Nitrogen-fixers present but no anchor plant — nitrogen disperses before it's usable"
  STABILITY_NEAR_MANDATORY_MISSING, "Acidic Soil Remediation Package strongly recommended for Venus Bio II"
  STABILITY_PREDATOR_NO_PREY, "Predator insects with no prey — composition will collapse"

Kardashev display:
  KARDASHEV_LABEL, "K {level}"
  KARDASHEV_073, "Pre-fusion civilisation"
  KARDASHEV_10, "Type I Civilisation"
  KARDASHEV_15, "First Era Complete"
  KARDASHEV_20, "Type II Civilisation"

Confirmation dialogs:
  CONFIRM_IRREVERSIBLE, "This cannot be undone."
  CONFIRM_POLAR_DETONATION, "Authorise polar detonation on Mars? This is permanent and will delay dome colonisation by ~40 years."
  CONFIRM_EUROPA_IMPACT, "Authorise Europa redirected impact? The impactor will arrive in ~50-70 years. The Hunt for Life window will close."
  CONFIRM_BIO_COMMIT, "Commit this composition? Once running, the phase cannot be redesigned unless it collapses."
  CONFIRM_YES, "Confirm"
  CONFIRM_CANCEL, "Cancel"

Bio phase labels:
  BIO_SLOT_FOUNDATION, "Foundation"
  BIO_SLOT_CATALYST, "Catalyst"
  BIO_SLOT_STABILISER, "Stabiliser"
  BIO_SLOT_BOOSTER, "Booster"
  BIO_SLOT_EMERGENT, "Emergent"
  BIO_SLOT_EMERGENT_LOCKED, "Locked — may activate mid-phase"
  BIO_DURATION_ESTIMATE, "Estimated duration: ~{years} years"
  BIO_STABILITY_LABEL, "Stability"
  BIO_TAG_LEAN, "This composition leans {tag}"
  BIO_TAG_NATURALIST, "Naturalist"
  BIO_TAG_ARCHITECT, "Architect"
  BIO_TAG_NEUTRAL, "Neutral"
  BIO_PROJECTION_PLACEHOLDER, "Biosphere projection: {planet} Phase {phase}"
  BIO_COUNTS_AS_3, "Counts as 3 organisms"
  BIO_NEAR_MANDATORY, "Strongly recommended for these conditions"
  BIO_AUTO_APPLIED, "Auto-applied"

--- PART 2: code conventions ---

Create src/autoloads/LocalizationManager.gd as a Godot 4 autoload singleton.

Responsibility: thin wrapper around Godot's built-in TranslationServer. Provides
helper functions for parameterised strings (Godot's tr() doesn't do substitution).

[→ Cursor: Godot's built-in tr("KEY") handles simple lookups. For strings with
 parameters like "Year {year}", we need tr() plus substitution. Explain this in a comment.]

func trf(key: String, params: Dictionary) -> String:
    # tr() the key, then substitute {param} placeholders.
    var result: String = tr(key)
    for param_key in params:
        result = result.replace("{" + param_key + "}", str(params[param_key]))
    return result

Example usage (add as a comment in the file):
  # Simple: tr("UI_PAUSE") → "Pause"
  # Parameterised: LocalizationManager.trf("UI_YEAR_LABEL", {"year": 2087}) → "Year 2087"
  # In scripts: always use tr() or LocalizationManager.trf() — never string literals in UI

After creating the CSV:
1. In Godot editor: Project → Project Settings → Localization → Translations → Add
2. Select data/localization/en.csv
3. Set the locale to "en"
Godot will auto-detect the CSV format and register all keys.

In Project Settings → Localization → Locale → Default, set "en".
```

---

# STEP 4 — Audio

## `src/autoloads/AudioManager.gd`

```
Create src/autoloads/AudioManager.gd for Helioscape. Godot 4 autoload singleton.

Responsibility: own all audio playback. Music layers, SFX, ambient loops. No audio
plays without going through AudioManager. Scenes never contain AudioStreamPlayer nodes
that play on their own.

[→ Cursor: explain why centralised audio: volume control, fade transitions, and ensuring
 music layers (e.g. calm exploration vs bio phase active) can crossfade cleanly. If every
 scene had its own players, coordinating this would be chaos.]

--- Architecture ---

Music uses three AudioStreamPlayer nodes (children of AudioManager):
  $MusicLayerA — primary music track
  $MusicLayerB — secondary layer (fades in/out for intensity changes)
  $MusicAmbient — ambient loop (planet-specific atmosphere sounds)

SFX uses an AudioStreamPlayer pool (pre-instantiated, reused):
  const SFX_POOL_SIZE: int = 8
  var _sfx_pool: Array[AudioStreamPlayer] = []
  [→ Cursor: explain pool pattern — avoids instantiating/freeing nodes at runtime.
   Pre-create 8 players in _ready(). When a SFX plays, find an idle player, assign the
   stream, play. If all busy, use the oldest (least-critical) and restart it.]

--- Placeholder streams ---

All audio paths are constants at the top of the file. All point to placeholder paths
that don't exist yet — AudioManager handles missing files gracefully (check File.file_exists
before loading, log a warning if missing, don't crash).

const MUSIC_SOLAR_SYSTEM: String = "res://assets/audio/music/solar_system_ambient.ogg"
const MUSIC_PLANET_EARTH: String = "res://assets/audio/music/earth_theme.ogg"
const MUSIC_PLANET_MARS: String = "res://assets/audio/music/mars_theme.ogg"
const MUSIC_PLANET_VENUS: String = "res://assets/audio/music/venus_theme.ogg"
const MUSIC_BIO_PHASE_ACTIVE: String = "res://assets/audio/music/bio_phase_layer.ogg"

const SFX_TECH_UNLOCKED: String = "res://assets/audio/sfx/tech_unlocked.wav"
const SFX_BIO_PHASE_COMMIT: String = "res://assets/audio/sfx/bio_commit.wav"
const SFX_BIO_COLLAPSE: String = "res://assets/audio/sfx/bio_collapse.wav"
const SFX_DISCOVERY: String = "res://assets/audio/sfx/emergent_discovery.wav"
const SFX_MILESTONE: String = "res://assets/audio/sfx/milestone.wav"
const SFX_CULTURE_EVENT: String = "res://assets/audio/sfx/culture_event_chime.wav"
const SFX_BUTTON_CLICK: String = "res://assets/audio/sfx/button_click.wav"
const SFX_PANEL_OPEN: String = "res://assets/audio/sfx/panel_open.wav"

--- Public API ---

func play_music(stream_path: String, crossfade_duration: float = 2.0) -> void:
    # Load and play on MusicLayerA, fading out any existing music.
    # [→ Cursor: use a Tween to fade volume of MusicLayerA to 0, then swap stream and
    # fade back in. If MusicLayerA is idle, just play directly with no fade.]

func play_music_layer(stream_path: String, volume_db: float = -6.0) -> void:
    # Add a music layer on MusicLayerB (bio phase active music, etc).
    # Fade in over 3 seconds.

func stop_music_layer() -> void:
    # Fade out MusicLayerB over 3 seconds.

func play_sfx(stream_path: String) -> void:
    # Play a one-shot SFX from the pool.

func set_master_volume(linear: float) -> void:
    AudioServer.set_bus_volume_db(AudioServer.get_bus_index("Master"), linear_to_db(linear))

func set_music_volume(linear: float) -> void:
    # Same but for music bus. Assumes a "Music" bus exists in the audio bus layout.
    pass

--- EventBus connections ---

Connect in _ready():
  EventBus.tech_node_unlocked → play_sfx(SFX_TECH_UNLOCKED)
  EventBus.culture_event_triggered → play_sfx(SFX_CULTURE_EVENT)
  EventBus.kardashev_milestone_reached → play_sfx(SFX_MILESTONE)
  EventBus.bio_phase_collapsed → play_sfx(SFX_BIO_COLLAPSE)
  EventBus.emergent_discovery_found → play_sfx(SFX_DISCOVERY)
  EventBus.bio_phase_started → play_music_layer(MUSIC_BIO_PHASE_ACTIVE)
  EventBus.bio_phase_completed → stop_music_layer()
  EventBus.planet_selected → _on_planet_selected (switch planet music theme)

func _on_planet_selected(planet_id: String) -> void:
    match planet_id:
        "earth": play_music(MUSIC_PLANET_EARTH)
        "mars":  play_music(MUSIC_PLANET_MARS)
        "venus": play_music(MUSIC_PLANET_VENUS)
        _:       play_music(MUSIC_SOLAR_SYSTEM)

Static typing throughout. Handle missing audio files gracefully — log a warning, never crash.
```

---

# STEP 5 — Debug console

## `src/ui/debug/DebugConsole.gd`

```
Create src/ui/debug/DebugConsole.gd for Helioscape.

Responsibility: hidden cheat/test console for development. Toggle with tilde (~) key.
Sits on CanvasLayer 4 (above everything). Invisible in release builds.

[→ Cursor: add a comment explaining why a debug console is essential for a strategy game
 with a 1000-year timeline. Testing the Dyson swarm at 80% completion by playing normally
 would take real hours. The console lets the developer jump to any game state instantly.
 This is a development tool only — it will be compiled out or hidden for release.]

--- Build detection ---

const IS_DEBUG: bool = OS.is_debug_build()

In _ready():
    if not IS_DEBUG:
        queue_free()  # remove entirely in release builds
        return
    # Register input action if not in InputMap
    if not InputMap.has_action("debug_console"):
        var event = InputEventKey.new()
        event.keycode = KEY_QUOTELEFT  # tilde/backtick
        InputMap.add_action("debug_console")
        InputMap.action_add_event("debug_console", event)

--- Commands ---

Implement a simple command registry. Each command: name + callable + description.

func _register_commands() -> void:
    _commands = {
        "help": _cmd_help,
        "year": _cmd_set_year,
        "speed": _cmd_set_speed,
        "tech": _cmd_complete_tech,
        "planet": _cmd_unlock_planet,
        "dyson": _cmd_set_dyson,
        "bio": _cmd_set_bio_phase,
        "milestone": _cmd_trigger_milestone,
        "ce": _cmd_trigger_culture_event,
        "resources": _cmd_set_resources,
        "kardashev": _cmd_set_kardashev,
        "stability": _cmd_set_bio_stability,
        "discovery": _cmd_trigger_discovery,
        "save": _cmd_save,
        "load": _cmd_load,
        "reset": _cmd_reset_game,
        "state": _cmd_print_state,
    }

Command implementations:

_cmd_set_year(args): GameState.game_year = float(args[0])
  Example: "year 2150"

_cmd_complete_tech(args): TechTreeSystem.unlock_tech(args[0], args[1])
  Example: "tech earth earth_fusion_ignition_theory"

_cmd_unlock_planet(args): initialise planet in GameState as if unlocked
  Example: "planet mars"

_cmd_set_dyson(args): GameState.dyson_coverage_percent = float(args[0])
  GameState.dyson_panel_count = int(float(args[0]) / 100.0 * DysonSystem.TOTAL_PANELS_FOR_100_PERCENT)
  Example: "dyson 80"

_cmd_set_bio_phase(args): set bio phase status for planet to "available" or "running"
  Example: "bio mars bio_3 running"

_cmd_trigger_milestone(args): KardashevSystem._complete_milestone(args[0])
  Example: "milestone type_1"

_cmd_trigger_culture_event(args): GameState.culture_event_queue.append(args[0])
  Example: "ce ce_mars_first_liquid_water"

_cmd_set_resources(args): set GameState.mercury_resources values
  Example: "resources 9999 9999 9999"

_cmd_set_kardashev(args): GameState.kardashev_level = float(args[0])
  Example: "kardashev 1.5"

_cmd_trigger_discovery(args): BioPhaseSystem._trigger_discovery(args[0], int(args[1]),
  DataManager.get_emergent_discoveries().filter(func(d): return d.id == args[2])[0])
  Example: "discovery mars 0 proto_soil_community"

_cmd_print_state(_args): print full GameState as JSON to output panel
  Example: "state"

--- UI ---

Full-width panel at the bottom of the screen (~200px tall). Dark bg_base background.
LineEdit for input at the bottom. ScrollContainer + Label for output above it.
Output: monospaced, text_primary. Errors in status_bad. Success in status_good.

Autocomplete: on Tab key, complete partial command names from _commands keys.

Add DebugConsole as a child of a new CanvasLayer (layer 4) in Main.tscn.
```

---

# STEP 6 — Register new autoloads

## Project settings update

```
Open project.godot and add the following autoloads, maintaining the existing order.
New autoloads go AFTER the existing five (DataManager, GameState, EventBus,
TimeManager, SaveManager).

New entries:
  LocalizationManager → res://src/autoloads/LocalizationManager.gd
  AudioManager → res://src/autoloads/AudioManager.gd

Also add BioPhaseSystem and MercuryBuildQueue as scene-level singletons (added to
the Systems node in Main.tscn), not autoloads — they depend on GameState being
initialised first, and they're complex enough to warrant scene-level lifecycle control.

Input map additions (add to existing input map):
  debug_console → KEY_QUOTELEFT (tilde/backtick) — handled in DebugConsole._ready()
  ui_zoom_out → Escape (already added)
  ui_open_history → H (already added)
  ui_pause → Space (already added)
  ui_speed_toggle → Tab (already added)
```

---

# STEP 7 — Web export setup (do in Godot editor)

```
Manual steps to configure Helioscape for web export (for demo sharing via itch.io).
These cannot be done via Cursor — they require the Godot editor UI.

1. Download export templates:
   Editor → Export → Manage Export Templates → Download and Install
   (requires an internet connection and ~500MB disk space)

2. Create a Web export preset:
   Editor → Export → Add → Web
   Settings to configure:
   - Export Path: build/web/index.html
   - HTML: Custom HTML Shell: leave as default for now
   - Texture Compression: check "Include VRAM Texture Compression for BPTC" and "S3TC"
     (needed for desktop browsers; mobile can be added later)
   - Extensions Support: leave off unless needed
   - Progressive Web App: leave off

3. Create a Desktop export preset (Windows as minimum):
   Editor → Export → Add → Windows Desktop
   - Export Path: build/windows/helioscape.exe
   - Architecture: x86_64

4. Add a build/ folder to .gitignore.

5. To export a demo:
   - Editor → Export → Select "Web" preset → Export Project
   - Upload the contents of build/web/ to itch.io
   - On itch.io: set "Kind of project" to HTML, set "Viewport dimensions" to
     your target resolution (1920×1080 recommended)
   - Enable "Fullscreen button" in itch.io embed settings

Notes:
- Web export requires SharedArrayBuffer, which itch.io enables automatically
  for HTML games. If hosting elsewhere, the server needs specific CORS headers.
- Audio may require a user interaction before playing in browsers (browser autoplay policy).
  AudioManager should handle this: connect any _input event to AudioServer.unlock()
  on first interaction.
- The PixelFilter SubViewport works correctly in web export — no special handling needed.
- Save files (user://) map to IndexedDB in web builds. SaveManager works without changes.
- Test the web build locally using Godot's "Run in Browser" button (requires a local server
  which Godot spins up automatically).
```

---

# Checklist: what to do before writing any UI

In order:
1. ☐ Create `.gitignore`
2. ☐ Set pixel art import defaults in Godot Project Settings (manual)
3. ☐ Create `assets/fonts/` folder, add placeholder mono.ttf and body.ttf
4. ☐ Run `CreateTheme.gd` as @tool, set theme in Project Settings (manual)
5. ☐ Create `data/localization/en.csv`, register in Project Settings (manual)
6. ☐ Create `LocalizationManager.gd` autoload
7. ☐ Create `AudioManager.gd` autoload
8. ☐ Create `DebugConsole.gd` and add to Main.tscn Layer 4
9. ☐ Download export templates (manual)
10. ☐ Now start UI prompts from v2 file

The UI_GUIDE.md should be in Cursor context for every single UI prompt from this point on.
```

---

*End of foundation prompts.*
*These establish the project scaffolding. Everything in v2 and bio-tech files builds on top.*
